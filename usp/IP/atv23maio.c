#include <stdio.h>

int max_str = 30;

int ordenar_alfab(char *str1, char *str2)
{
    for (int i = 0; i < max_str; i++)
    {
        }
}

int main()
{
    char str1[max_str];
    char str2[max_str];

    printf("String 1: ");
    scanf("%s", str1);
    printf("String 2: ");
    scanf("%s", str2);

    ordenar_alfab(str1, str2);
}